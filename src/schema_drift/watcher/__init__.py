"""Source watchers — poll source systems and emit ``SchemaSnapshot`` / ``RawChange``.

Concrete implementations:
    * ``PostgresWatcher`` — Week 1
    * ``RestApiWatcher`` — Week 4
    * ``DebeziumWatcher`` — Week 7 (stretch)
"""
